package services.add_track_to_playlist;

public class AddTrackToPlaylistController {
    public void execute(String id, String username, String playlistName) {
        //todo 1. add track with "id" to playlist with name "playlistName" owned by user with username "username"
        //todo 2. if playlist with name "playlistName" owned by user with username "username" does not exist, then do nothing
    }
}
